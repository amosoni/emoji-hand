"use client";

import { useState } from "react";

import { api } from "~/trpc/react";

export function LatestPost() {
  // 已移除 api.post.getLatest.useSuspenseQuery 和 api.post.create.useMutation 相关代码

  return (
    <div className="w-full max-w-xs">
      {/* 已移除 api.post.getLatest.useSuspenseQuery 和 api.post.create.useMutation 相关代码 */}
    </div>
  );
}
